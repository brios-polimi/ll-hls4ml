[project]
name = "ll-hls4ml"
version = "0.1.0"
description = "Surrogate models for HLS resource/latency prediction from CDFG graphs"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch",
    "torch-geometric",
    "numpy",
    "tqdm",
    "pyyaml",
]

[project.optional-dependencies]
analysis = [
    "pandas",
    "networkx",
    "scikit-learn",
    "matplotlib",
    "seaborn",
    "scipy",
]
notebooks = [
    "ipython",
    "jupyter",
    "ipywidgets",
    "pandas",
    "networkx",
    "scikit-learn",
    "matplotlib",
    "seaborn",
    "scipy",
]
dev = ["pytest"]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
